# Moved to monorepo root

The active React application is now at:

`../../../../frontend/`

The Django API is at:

`../../../../backend/`

Run the frontend from `gradeportal2/frontend/` and the API from `gradeportal2/backend/`.
See the root [README.md](../../../../README.md).
